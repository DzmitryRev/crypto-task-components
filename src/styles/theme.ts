/* eslint-disable linebreak-style */

export type ThemeType = {
  overflow: string;
};

export const defaultTheme: ThemeType = {
  overflow: 'auto',
};

export const openedModalTheme: ThemeType = {
  overflow: 'hidden',
};
